{% extends 'base.html.twig' %}

{% block body %}
    <h1>Materialsofconstruction</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ materialsOfConstruction.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ materialsOfConstruction.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ materialsOfConstruction.type }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ materialsOfConstruction.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('product_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('product_edit', { 'id': materialsOfConstruction.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
