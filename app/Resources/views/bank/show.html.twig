{% extends 'base.html.twig' %}

{% block body %}
    <div class="">
        <a href="{{ path('bank_index') }}" class="btn btn-success left"><i class="fa fa-chevron-circle-left"></i> Retour</a>

        <div class="clearfix"></div>
        <div class="separator"></div>
        <div class="row">
            <div class="col-md-12 col-xs-12">
                <div class="x_panel">
                    <div class="x_title">
                        <h2>Consultation banque "{{ bank.name }}"</h2>
                        <div class="clearfix"></div>
                    </div>
                    <div class="x_content">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nom</th>
                                <th>Téléphone</th>
                                <th>Email</th>
                                <th>Adresse</th>
                                <th>Action</th>
                            </tr>
                            </thead>
                            <tbody>
                            <tr>
                                <td>{{ bank.id }}</td>
                                <td>{{ bank.name }}</td>
                                <td>{{ bank.phone }}</td>
                                <td>{{ bank.mail }}</td>
                                <td>{{ bank.address }}</td>
                                <td>
                                    <a href="{{ path('bank_edit', { 'id': bank.id }) }}" class="btn btn-sm btn-warning" type="button" data-placement="top" data-toggle="tooltip" data-original-title="Modifier"><i class="fa fa-edit"></i></a>
                                    {{ form_start(delete_form) }}
                                    <button type="submit" class="btn btn-sm btn-danger inline" data-placement="top" data-toggle="tooltip" data-original-title="Supprimer"><i class="fa fa-trash"></i></button>
                                    {{ form_end(delete_form) }}
                                </td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
